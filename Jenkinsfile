pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World from ashfaq"'
                sh '''
                    echo "Multiline shell steps works too im still working...."
                    ls -lah
                '''
            }
        }
    }
}
